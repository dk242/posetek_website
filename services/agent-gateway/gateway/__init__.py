"""Agent gateway — the Cloud Run service implementing
``docs/LLM_GATEWAY_CONTRACT.md`` from the PoseTek-mobile-app repo.

That document (and ``docs/LLM_PLATFORM_PLAN.md`` alongside it) is the binding
wire contract and design rationale; this package is the gateway half. The app
half lives in ``KickAI/LLM/`` in the mobile repo.
"""
