# Admin plan generation repair — 2026-09-08

Deployed and production-data verified. Shared `buildStatsSnapshot` now emits shooting evidence under the canonical `kick` drill ID, including missing-drill labels. It previously sent `deadballShot`, causing all eleven positioned VUSC players' requests to fail with `ballSpeed must belong to kick`. Other recording/Storage aliases stay unchanged. Six-primary-metric regression coverage was added.

All 534 website tests and the TypeScript/Vite production build passed. Lint completed with existing warnings. Production Netlify deploy `6a9fbc2cac84dcde60e6b0bf` serves the fix; `/admin/programs` and the changed bundles matched the tested files byte for byte. The live baseline was already commit `a5b9580`, so the release added only this runtime fix.

The real batch preserved the eleven original player requests: two weeks, two sessions weekly, and a 60-minute budget. Ten completed immediately. A repeated generated-workout ordering rejection for the remaining player was fixed separately in backend commit `47b5aa2`, deployed as `agent-gateway-order-f6f286365f06`; the checker stayed enabled. The last player then completed. All eleven active plans and all 44 workouts passed persisted-plan checks, with estimated sessions of 58–60 minutes, correct assigned positions, unchanged available primary best-result evidence, one active plan each, generation contexts and schedule counters. Existing failed jobs were retained; successful players were not regenerated.

The batch used authenticated Firestore pending-job submission through the real Eventarc/gateway/model/persistence path. Browser/Computer Use bootstrap was unavailable, so no signed-in browser click-through is claimed. Full player-level receipts remain at `/private/tmp/posetek-vusc-plans/build-receipt.json`. Backend source/deployment/test evidence is under `Services/agent-gateway/deployments/2026-09-08/vusc-ordering` in `python-video-processor`. No remote Git push was performed.
