# Conversion and account-entry candidate (2026-09-26)

Branch: `codex/web-conversion-entry-20260926`. This is source work only; it has not been deployed or exercised against production Auth/Firestore.

The existing `/bookPerformanceTest.html` page and case-insensitive `/bookperformancetest` rewrite now explain an enquiry. All Home/header/footer entry points still reach the same URL and say “ask” rather than claim a reservation. Contact uses the existing Dylan address, with a copy-email fallback. There is no price, time inventory, checkout or claimed confirmed booking. A reservation and payment flow needs a separately reviewed service contract.

Legacy organization signup keeps the Auth identity after any callable failure. The client reads the same UID's canonical profile and referenced organization roster before considering admission complete, and a retry reuses the same signed-in UID. The `createOrganization` transaction returns an existing organization for that coach UID so a lost response cannot mint a second club. Existing join uses `arrayUnion`. Production rules must be verified to permit the member's profile and organization readback; browser acceptance should inject both pre-commit refusal and post-commit response loss for player join, coach join, and coach create.

Independent coach signup writes its UID document in a Firestore transaction that preserves any populated profile. Email verification is attempted after that write; an email failure no longer strands provisioning. A session-scoped pending intent stores name and email tied to the Auth UID and reopens signup after refresh, allowing a same-account retry if the profile write failed. It stores no password. Browser acceptance should inject profile-write and email failures, reload, and confirm one coach document with no overwritten fields. If session storage is cleared, the signed-in user can re-enter name and email.

“Remember me” now selects Firebase LOCAL or SESSION persistence before sign-in; checked is the intentional default. Password reset and organization setup have dialog labels, focus ownership, Enter submission and topmost Escape. The homepage pose viewer has a pause/play control, and choosing a pose stops automatic cycling until resumed.

Initial validation used a partial shared dependency cache. The second pass below installed the declared lockfile dependencies in this isolated checkout and supersedes those build and browser limitations.

## Second local acceptance pass

The standard `npm --prefix app ci --no-audit --no-fund` restored the repository's declared dependencies in this isolated worktree. `npm --prefix app run build:marketing` and `npm --prefix app run build` now pass. Chrome headless checks against local preview/dev servers pass at 390px and 1440px: booking page, copy-email clipboard result, mailto target, lower-case URL aliases, Home booking links, zero horizontal overflow, hero pause and pose stability. The keyboard check confirms forgot-password dialog focus/Tab wrap/Enter/Escape and nested organization focus/Enter validation/topmost Escape. It blocks all Identity Toolkit HTTP requests; no real password reset or signup was submitted. `node --test scripts/conversion-entry-browser.test.mjs` runs these three interactive checks with both local servers running; set `POSETEK_CHROME` for another Chrome binary.

The second pass found two release/preview gaps: the preview served the old reference checkout, and the guarded release builder would have retained the old lowercase booking file. Preview now serves the current source at the CamelCase and lowercase aliases. The release builder verifies every pinned baseline file first, then installs only the reviewed enquiry at `/bookperformancetest.html`. Application-release composition checks that one replacement against the exact reviewed source; a dedicated test rejects an unreviewed substitution. Netlify has explicit rewrites for the historical CamelCase and clean lowercase links before the SPA fallback. The no-JavaScript Home link now says “ask.”

`node scripts/build-production.mjs` was attempted after a clean marketing build but stopped at the existing `/drillcreation.html` preservation checksum: the pinned historical byte differs from the currently downloadable live byte and no matching local source was available. The baseline manifest expects 36,092 bytes / SHA-1 `1aea46bb035974a1adf4a90bb8c789126d656631`. The local source is 35,175 bytes / `ae35c4489fe37249044d749e1ccb6ded496c4e3e`; both current production and the pinned historical deployment serve the same 36,088 bytes / `4b51adb5329e4c902660d449e61a7842333e6b50`. This shows no new drift between current production and the pinned deployment; the mismatch is between pinned original bytes and served HTML, with a separate local-source difference. It is unrelated to the testing-page replacement and remains a release blocker until the baseline is reconciled through the repository's documented process. No guard was bypassed. The release artifact was not accepted or deployed.

Synthetic tests cover the organization create/join replay, independent profile write rejection before commit and response loss after commit, email-send failure, same-UID resume and preservation of a populated coach document. They use local in-memory fakes; they do not prove production Firestore rules or live provider behavior. Production rule readback and a real browser acceptance on a disposable emulator account remain outstanding before release.
